---
import _Article from './Article.astro';
import { SAMPLE_ARTICLES as _SAMPLE_ARTICLES } from '../config';
import styles from '../styles/components/ArticleIndex.module.css';
---

<div class:list={styles.page}>
  <div class:list={styles.content}>
    <!-- {
      SAMPLE_ARTICLES.map(article => (
        <Article
          title={article.title}
          time={article.time}
          content={article.content}
          category={article.category}
          url={article.url}
          cover={article.cover}
          id={article.id}
        />
      ))
    } -->
  </div>
</div>
